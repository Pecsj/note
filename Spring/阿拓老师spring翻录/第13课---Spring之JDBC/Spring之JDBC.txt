
	
	知识回顾
	  IOC(DI)
		控制翻转思想   依赖注入(对象中的属性 value ref)  c: p: util:
	  SpEL
		#{}  操作对象  属性  方法   逻辑  计算
	  AOP面向切面的编程思想
		OOP区别  编程写法<aop:>  切面对象 切入点 通知建议

	SpringJDBC

	JDBC的六部曲
	  1.导包(数据库驱动MySQL)
	  2.加载驱动类	类名字
	  3.获取连接	url?字符集 账号 密码
	  4.创建状态参数(输入输出流)  读写数据库
	  5.执行操作
		增删改int count = executeUpdate();
		查询ResultSet = executeQuery();	处理查询结果(结果集的值取出来 存入新的容器)
	  6.关闭资源














