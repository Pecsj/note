
	
	利用JavaSE部分实现  模拟QQ聊天小系统

	1.Socket通信(TCP/IP)------套接字
		建立连接
	2.输出流、输入流(I/O)
		发送数据，接收数据
		读取数据的单位(一个字节 一个字符)
	3.客户端、服务端
		客户端(多)，服务端(一)
		多线程
	4.面向对象编程思想
		集合Map，List
		字符串String
		继承关系，面向对象思想
	5.JavaSE部分的小窗体#
		GUI----Swing(#)

	=====================================================
	已经简单的实现了
	1.服务端与客户端可以进行通信了
	2.产生几个问题	exe4j
		发送和接收是有顺序的(多线程 发送 接收 并行)
		每一次只能说一句  两端等待的位置不一样
		服务端只有一个  多个客户端是否能连接到一个服务器上????
			如果可以连上 服务端就会产生多个socket该怎么存储
		

	刚才的小问题
	解决了一个
	服务端通过循环，可以接收好多客户端信息
	每一次一个客户端成功访问我  会得到一个socket对象
	选择集合来进行存储(集合长度创建以后可以改变)

	集合
	List有序可重复
	Set无序无重复
	Map存储结构是key-value   key无序无重复 value无序可重复
	set无重复  值?  Socket  服务端存储起来这么多socket目的呢?
	List 适合群聊




	map<key,value>

	map<String,User>


		socket = map.get(key);

		key---key+value

		socket创建输出流
		输出流.println(id:+message);










